Flask==3.0.3
requests==2.32.3
numpy==1.26.4
pandas==2.2.2
scikit-learn==1.5.1
openai==1.54.4
# httpx 0.28 removed the `proxies=` arg that openai 1.54.x still passes (and
# PythonAnywhere's proxy env vars make openai try to use it), which crashes
# client construction with: Client.__init__() got an unexpected keyword
# argument 'proxies'. Pin below 0.28. (Alternatively bump openai>=1.55.3.)
httpx==0.27.2
